# Nightlight Recovery Science Foundation Context

## Current truth

- This isolated worktree began clean and detached at the required baseline `6b3de4ee97c5391084538bec84db3b1a1f4e05ed`; the default `main` worktree pointed to the same commit at startup.
- The implementation owner may edit, test, stage, and commit only the delegated science-foundation paths. Integration, refs, remote state, registry, deployment, and worktree topology remain owned elsewhere.
- Existing readiness rules produce observation and coverage quality points plus public readiness bands. They are admission evidence, not observed recovery outcomes, forecasts, probabilities, grades, or ranks.
- Existing source and reproducibility manifests already identify VNP46A2 and EAGLE-I, but record Earth Engine/export-receipt and local derivative-lineage gaps.
- The public contract remains no-score/no-rank/no-outcome. This lane does not modify `project/nightlight-public/**` or `project/nightlight-dashboard/**`.

## Decisions and deviations

| Time | Evidence or decision | Impact |
| --- | --- | --- |
| 2026-08-11 start | Exact baseline and clean detached worktree verified before writes. | Implementation authorized within the delegated paths. |
| 2026-08-11 start | Multi-worktree inventory shows concurrent detached lanes; this agent is not integration owner. | Deliver a committed package only; do not update registry or integrate. |
| 2026-08-11 start | Existing raw and restricted inputs are outside the delivery contract. | Use tracked metadata and deterministic fixtures only; no raw/cache traversal or Git additions. |
| 2026-08-11 NL-R01 | Outcome contract and dependency-light derivation passed six targeted tests. | Construct is frozen but remains explicitly non-admitted; readiness, observed outcomes, forecasts, and probabilities cannot be relabeled. |
| 2026-08-11 NL-D01 | Official metadata plus tracked receipts separate EAGLE-I, VNP46A2, OE-417, EIA-861, and direct utility alternatives. | Label pilot is blocked on parent/transform receipts, event time, denominator, zero-versus-gap semantics, and rebuildable independent ground truth. |
| 2026-08-11 NL-C01 | Nine complete same-family events were tested across fixed-max and cohort normalization, weighting alternatives, LOO, and 2,000 seeded Monte Carlo weight draws. | Research composite is `no_go`: sample count is below 12 and maximum Monte Carlo rank span is 4 versus a limit of 2. No Public export is allowed. |
| 2026-08-11 NL-L01 | Deterministic admission evaluation remained blocked and the pilot manifest contains zero events and labels. | No mock labels or forecast/probability training occurred; the five-step handoff is executable after external evidence is restored. |
| 2026-08-11 validation | 21 passed/1 existing private-output skip, 37 related gate passes, 6 stage gate passes, reviewed-modeling ready, and full-upstream blocked on seven known gaps. | Available gates support the bounded contracts; `test_analysis_contracts.py` remains unrun because no current interpreter combines pytest with statsmodels. |
| 2026-08-11 overlap audit | The current Public capability worktree changes only `project/nightlight-public/**` plus its own task records; it has no exact path overlap with this lane. No base-aligned sibling showed Dashboard changes at audit time. | Relationship is semantic, not textual: integrate science definitions first, then Public/Dashboard copy or UI, and rerun combined boundary tests. |
| 2026-08-11 commit | Implementation Lore commit `46dfab512e24402d70afce88729137281a7e44fa` was created directly on detached HEAD with the required baseline as parent; message, stat, author scope, and clean post-commit status were read back. | Science implementation is committed and ready for integration; this subsequent docs-only closeout records that fact without changing implementation behavior. |
| 2026-08-11 Dashboard handoff | Source task `019fef6d-8206-7402-a53f-1c9b5e52edea` transferred the legacy facility-probability ambiguity into this science lane. Read-only review confirmed the exporter serializes a mean-aggregation `NaN` as numeric `0.5`, while the 25 legacy arrays carry neither `schemaVersion` nor missingness provenance. | Re-open this task as NL-P01. Keep Dashboard/Public files read-only; own the future producer contract, legacy migration boundary, and preprocessing assessment here. |
| 2026-08-11 preprocessing audit | The same ten-feature `fillna(0)` operation appears in the LOEO, full-panel training, probability-map regeneration, and Dashboard export paths. A read-only audit of the tracked 61,903-row, 25-event panel found zero missing values in the five source columns and ten engineered features examined. | Treat zero-fill as implementation-consistent and a no-op for this bounded tracked panel, not as an admitted general preprocessing contract. Model artifacts lack a fit-time preprocessing/lineage receipt and the code has no missing indicators, so future scientific admissibility remains blocked. |
| 2026-08-11 NL-P01 contract | A v1 probability is a discriminated observation: only `available` may carry a finite `[0,1]` value and it requires eligible/finite pixel counts; `unavailable`, `not_assessed`, `computation_failed`, and `validation_failed` use null plus a status-bound reason. Artifact-level source versions and receipts, event/facility provenance, and aggregation rules are required. | A true modeled `0.5` is now representable without collision. No legacy record is reclassified and no producer/Dashboard code is changed in this lane. |
| 2026-08-11 legacy migration | The 25 arrays remain read-only `legacy-v0`; all numeric values, including the ten `0.5` values, remain unchanged and ambiguous. The v1 path requires regeneration from pinned facility, pixel-probability, preprocessing, model, and producer lineage. | Dual-read ends only after 25/25 regeneration, Dashboard adapter conformance, and one complete compatibility release; retirement is no earlier than the following major consumer release after a manifest proves no v0 production references. |
| 2026-08-11 NL-P01 validation | Twelve targeted conformance/negative/round-trip tests passed. The affected science, readiness, Dashboard contract, modeling entrypoint, source/reproducibility, and stage group passed 90 tests with one existing private-output skip. Reviewed-modeling remained ready; full-upstream remained blocked on the same seven external/lineage gaps. | The contract and bounded assessment are validated locally. This is not producer migration, model recalibration, restored lineage, or scientific admission of zero-fill. |
| 2026-08-11 NL-P01 commit | Continuation Lore commit `c3b18c6613f8c915721021d5e45367f3b0ef5f02` was created on detached HEAD with parent `a407de6474d678cbb0bd9e8566ffd66c451f6a52`; full message, stat, scope, ancestry, and clean post-commit status were read back. | NL-P01 implementation is committed and ready for integration. This docs-only closeout records exact delivery evidence without changing the producer contract. |

## Live process ownership

| Process | Owner | Log path | State |
| --- | --- | --- | --- |
| None | N/A | N/A | No long test, server, browser smoke, or shared process started. |

## Handoff

The future integration owner should apply this science-foundation candidate before Public or Dashboard changes that consume recovery definitions. At the 2026-08-11 audit, the active Public capability lane had no exact file overlap. If a later Public/Dashboard candidate changes `evidencePassportManifest.json`, its schema/window family must be compared with this lane's bounded snapshot and NL-C01 rerun; do not resolve drift by exporting internal scores, ranks, or labels. Dashboard must retain legacy-v0 numeric values unchanged and treat numeric `0.5` as ambiguous until a conforming v1 artifact is regenerated from pinned producer inputs and model lineage. A Dashboard v1 adapter must branch on `schemaVersion`, read numeric values only for `status: available`, render null states from status/reason, and keep the legacy limitation throughout dual-read; it must never wrap or infer v0. Public needs no direct change. If a future Public freshness/error contract needs the boundary, reuse null-versus-available and the high-level status classes, but keep facility-specific reasons producer-scoped and do not expose probabilities, recovery labels, scores, or ranks. After all candidates land, rerun the science tests plus each surface's own validation. The older `591a` worktree has a broad historical delta from this base and should not be treated as a current cherry-pick target without separate integration-owner review.

## Next step

Future integration owner applies the final detached-HEAD candidate range after the exact required base, preserves the active records until integration is complete, and runs the combined science plus Public/Dashboard validation in the recommended order.
