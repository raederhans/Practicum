# Context

## Current truth

- Local repository root: `C:/Users/raede/Desktop/essay help master/Practicum`.
- Current branch: `codex/personal-project-sync`; branch base: `main@1e3bcdade293b7e4c87ec4e00807cf3e86711bbc`.
- Working tree was clean at audit start.
- `origin` points to `raederhans/Practicum`; `teammate` points to `ZhiyuanZhaoMicheal/Practicum`.
- Fresh Git evidence reports only the current worktree, and `.git/worktrees` does not exist. This audit found no current administrative worktree records to prune or synchronize.
- Fresh remote evidence places `origin/main` at `1e3bcda` and `teammate/main` at `1f63e19`.
- The local clone is shallow. It contains no provable merge base between the two tips, so the apparent 21-versus-35 commit divergence is incomplete history and neither side can currently be treated as a safe fast-forward. This does not prove that the complete histories are unrelated.
- Tree comparison: local `main` has 740 tracked files; teammate `main` has 2,416. There are 601 local-only paths, 2,277 teammate-only paths, and 139 shared paths.
- The teammate README explicitly attributes Stage 0 NTL download, Stage 1 EDA, and Stage 1.5 OLS/MixedLM/Logit/Cox work to Qiushi Yu; it attributes Stage 2/3, dashboard, figures, and reports to Zhiyuan Zhao.
- The public site is live at `https://zhiyuanzhaomicheal.github.io/Practicum/`. Its overview, MapLibre map, technical documentation, and presentation route are backed by `project/nightlight-dashboard/` and deployed by `.github/workflows/deploy-dashboard.yml`.
- Live map verification showed 24 selectable locations, 25 events, five basemaps, probability layers, and facility overlays; no application errors were observed, only headless WebGL performance warnings.
- The public narrative is internally inconsistent: overview text says 19 U.S. states, while `MODELS.md` and detailed docs say 17 U.S. states plus Turkey. Direct counting of the exported event configuration supports 15 U.S. states plus Puerto Rico plus Turkey for the full 25-event display; the Stage 3 panel covers 22 events across 15 U.S. states. Neither public headline is currently supported by the checked-in data. `SlidesView.vue` shows R-squared 0.475 and a 38% facility-density figure, while the result JSON, `MODELS.md`, and detailed docs support R-squared 0.7472 and a 63% ratio.
- Full reproducibility is not possible from Git alone. The teammate README lists separately transferred raw data and three omitted large datasets (~820 MB ZCTA, ~130 MB counties, ~57 MB IBTrACS); the local `.gitignore` also excludes several NTL, NLCD, WorldPop, cloud, and model-output artifacts.
- On 2026-08-05 the user confirmed teammate reuse permission and authorized necessary software installation, official-data downloads, program execution, and experiments for synchronization Steps 3–5.
- Implementation branch: `codex/personal-project-sync`, created from unchanged local `main@1e3bcdade293b7e4c87ec4e00807cf3e86711bbc`.
- Integration owner: the primary agent. Subagents are read-only evidence owners and may not change Git refs, index, worktree topology, or remotes.
- Isolated history reference: `C:/Users/raede/.codex/tmp/practicum-reference-20260805`. It is non-shallow and contains `origin/main@1e3bcda` plus `teammate/main@1f63e19`.
- Complete-history merge base: `7b0445fb184731985b265eccf44582257c6f5c2f` (`merge data collection`, authored by `yu qiushi`). After that base, origin has 17 unique commits and teammate has 40; neither tip is an ancestor of the other.
- Complete-history author grouping for the unique ranges: origin-only has 12 `yu qiushi` commits plus 5 local `root` commits; teammate-only has 40 `Zhiyuan Zhao` commits. Author identity is evidence, not a substitute for path-level provenance review.
- The current tree tracks 469 paths under `project/data/raw/**`, including EAGLE-I outage CSVs. Official-source review classifies EAGLE-I as partner-restricted and does not establish raw-data redistribution rights. Keep these files available for local experiments, but block public push until tracked-file handling and any history-remediation decision are reviewed separately; do not delete or rewrite them implicitly.
- The personal branch now uses a consistent publication contract: Stage 2 covers 25 events in 17 jurisdictions (15 U.S. states, Puerto Rico, and Turkey); Stage 3 covers 1,002 ZIP-event observations across 22 events in 15 U.S. states.
- The formal Stage 3 rerun uses ensemble probability TIF band 3 and EPSG:5070. M1+ has in-sample R-squared 0.7603, adjusted R-squared 0.7543, and n=977. These are descriptive fit statistics, not predictive accuracy or a causal estimate.
- The donor 0.7472/0.7408/0.63 values are retired provenance because they use RF band 1 and EPSG:3857. The regenerated severity-tertile ratio is 0.551, but event/county-event clustered checks do not support an equity claim; the dashboard labels it descriptive-only.
- `PROJECT_PROVENANCE.md` records the collaborative origin, contributor boundary, donor commit, and third-party-data release gate. The dashboard leads with Qiushi Yu's personal continuation and preserves Zhiyuan Zhao attribution.
- The reviewed implementation is committed locally as `acc4b2def96a4e653a604f33e8f462472fe1671d`; it is one commit ahead of `origin/main` at the time this closure record is prepared. No push, merge, or deployment occurred.

## Gap classification

| Area | Local `main` | Teammate `main` | Classification |
| --- | --- | --- | --- |
| Stage 0/1 collection and EDA | Present, including local notebooks and VIIRS/EAGLE-I assets | Cleaned/reorganized versions present | Preserve local; reconcile naming only |
| Stage 1.5 interpretation | Strong local pipeline, reports, notebooks, WSL entry point | Merged subset and handoff docs | Preserve local as source of truth |
| Stage 2 pixel prediction | Largely absent | Model D notebooks/scripts, LOEO, model files, probability maps | Missing locally; copy only after explicit permission, otherwise clean-room reimplement |
| Stage 3 ZIP regression | Largely absent | ZIP panel, full/extra regression scripts and JSON results | Missing locally; copy only after explicit permission, otherwise clean-room reimplement |
| Interactive dashboard | Absent | Vue 3, Vite, MapLibre, static deploy data | Missing locally; reference design unless explicit reuse permission is obtained |
| Deployment | Absent | GitHub Pages workflow | Missing locally; reusable after repository-path changes |
| Figures and final presentation | Older/local report assets | `docs/pre_figures`, `docs/year_end_show`, report/deck assets | Compare and curate; do not overwrite local originals |
| Raw and generated data | Local subset (~0.25 GB) plus ignored local-only artifacts | Large processed/result tree, but raw data still incomplete | Build a manifest; do not treat Git as complete data transfer |
| Legacy notebooks/documents | Present locally | Many deliberately removed in handoff cleanup | Archive selectively; not a feature gap |

## Recommended synchronization sequence

1. Keep local `main` unchanged as the local evidence-preserving baseline, not as proof of complete authorship, and create a new personal-project branch for implementation.
2. Treat `teammate/main@1f63e19` as a read-only donor snapshot; do not wholesale-merge the two tips while history is incomplete.
3. Before any integration decision, fetch complete histories in an isolated reference clone, recompute merge bases and authorship evidence, and keep selective import as the default even if a common ancestor is found because the handoff contains broad deletions/reorganization.
4. Establish a reuse gate. Obtain explicit permission covering copy, modification, public redistribution, attribution, and the intended license for any teammate-authored code, prose, image, or generated asset. Without permission, keep only links and observations and use a clean-room rewrite.
5. Create a machine-readable result/source manifest before copying data or results. Resolve the geographic scope and 0.475/0.7472 and 38%/63% conflicts from checked-in result files.
6. Protect these local paths from donor overwrite: `README.md`, `.gitignore`, `project/modeling/**`, `project/modeling_tracking/**`, `project/modeling_report/**`, `notebooks/**`, `document/**`, `extra data/**`, `POI/**`, `run_wsl_modeling.ps1`, and existing local data.
7. Do not import broad donor paths such as `project/**`, `project/script/**`, `project/data/**`, or `docs/**`. After permission, review bounded slices separately:
   - Dashboard source: `project/nightlight-dashboard/package*.json`, `vite.config.js`, `index.html`, `src/**`, and non-data public assets.
   - Dashboard static data: `project/nightlight-dashboard/public/data/**`, only after provenance, redistribution, checksum, schema, event-ID, and CRS checks.
   - Stage 2/3: only explicitly named `stage2_*`, `stage3_*`, Model D regeneration/LOEO scripts, and compact canonical result tables; no blanket directory copy.
   - Deployment: `.github/workflows/deploy-dashboard.yml` last, after repository name/base path, local build, routes, assets, attribution, and external-publish approval are settled.
8. For every slice, review the allowlist diff and verify input/output lineage, dependency completeness, file size/checksum/schema/event IDs/CRS, absence of mock or fallback data, and canonical metrics. For the dashboard, require `npm ci`, `npm run build`, route/static-asset checks, and result-text assertions before enabling deployment.
9. Make the project personally distinct through a new user-authored extension, such as a reproducible event-addition workflow, calibration/uncertainty analysis, or a newly designed dashboard generated from the user's pipeline.

## Decisions and deviations

| Time | Evidence or decision | Impact |
| --- | --- | --- |
| 2026-08-05 | The requested first phase is a read-only comparison. | Do not merge, rewrite, push, deploy, or clean Git metadata during this audit. |
| 2026-08-05 | Multiple remotes are configured, while the fresh worktree inventory contains only the current checkout and no `.git/worktrees` directory. | Treat remote comparison as an integration-risk review, but do not claim any stale or removed worktree without evidence. |
| 2026-08-05 | The shallow histories have no locally provable merge base and the teammate handoff deliberately deletes/reorganizes many local assets. | Block merge until complete-history inspection; even afterward, selective import remains the default risk-control mechanism. |
| 2026-08-05 | Public authorship notes assign Stage 2/3/dashboard to the teammate, and neither repository has a LICENSE file. | Do not copy or republish teammate-authored material without explicit permission; attribution alone is insufficient. |
| 2026-08-05 | Result JSON supports R-squared 0.7472 and facility-density ratio 0.63. | Treat the 0.475 and 38% slide values as stale until a contrary reproducible result is found. |
| 2026-08-05 | Checked-in event configuration supports 15 U.S. states plus Puerto Rico plus Turkey; Stage 3 covers 22 events across 15 states. | Replace both the 19-state and 17-state public claims after regenerating a canonical scope table. |
| 2026-08-05 | The user confirmed teammate permission and requested implementation of synchronization Steps 3–5. | Donor slices may now be imported with attribution and provenance; broad merge and unverified data copying remain disallowed. |
| 2026-08-05 | Local `main` must remain the evidence-preserving baseline. | Work proceeds on `codex/personal-project-sync`; public push and Pages deployment remain gated until local verification is complete. |
| 2026-08-05 | The isolated full-history clone proves a common base but also proves 17-versus-40 unique commits with neither tip ancestral to the other. | Do not fast-forward or wholesale merge; preserve the local branch and import donor paths through explicit allowlists with per-slice provenance. |
| 2026-08-05 | The current Git tree already tracks partner-restricted EAGLE-I raw CSVs. | Local analysis may continue, but public publication is blocked until raw-data redistribution and history handling are explicitly resolved; no automatic deletion or history rewrite. |
| 2026-08-05 | Independent statistical review rejected the donor/current Stage 3 results as causal, equity, or uncorrected spatial evidence. | Use event-clustered OLS, event-blocked KNN, and explicit not-identifiable results; publish only descriptive fit and scope facts with caveats. |
| 2026-08-05 | The ensemble/Albers formal rerun produced M1+ R-squared 0.7603/adjusted 0.7543 (n=977); clustered severity and income sensitivities are not significant enough for the old narrative. | Retire 0.7472/0.7408/0.63 as donor provenance, remove causal/equity claims, and record 0.551 only as descriptive sensitivity. |
| 2026-08-05 | Final Chromium smoke found a stale-preview 404 after rebuilding while the server was running; restarting from the final `dist` removed it. | Long-lived preview must be restarted after every production rebuild; final routes/assets were rechecked with zero console errors and HTTP 200 assets. |
| 2026-08-05 | Final code review found a clipped 25-event LOEO chart, empty-CSV failure, Moran row misalignment, weak probability validation, duplicate map clicks, and blanket warning suppression. | Lock each failure with a focused test, repair the narrow root cause, rerun formal outputs, and repeat browser acceptance before committing. |
| 2026-08-05 | Security review verified that the public origin already exposes 52 partner-restricted EAGLE-I CSVs (215,544,230 bytes) and an unauthenticated raw URL returns HTTP 200. | Correct the release controls locally, but keep public push/deploy closed; a deletion-only commit cannot retract public Git history. |
| 2026-08-05 | Final code review initially found glyphless-style labels and failed-basemap recovery gaps; the repair also exposed a stale-map asynchronous data race in real Chromium. | Bind detail loads to their owning map instance, require a loaded style before layer writes, omit all text layers without glyphs, clear the global map reference before teardown, and verify both event-loaded Satellite switching and post-failure recovery before committing. |
| 2026-08-05 | Final independent code re-review returned APPROVE with zero blocking findings. | Create the local Lore commits, but do not convert local verification into publication approval. |

## Live process ownership

| Process | Owner | Log path | State |
| --- | --- | --- | --- |
| Full-history reference clone/fetch | Primary agent | `C:/Users/raede/.codex/tmp/practicum-reference-20260805.clone.log` and `.teammate-fetch.log`; clone used `--filter=blob:none --no-checkout`, then fetched teammate heads | Complete: both commands exit 0; reference is non-shallow; both tips and merge base verified |
| Census TIGER/Line 2020 source download | Primary agent | `C:/Users/raede/Desktop/essay help master/Practicum/cache/logs/census-tiger-2020-download.log`; official archives are stored in ignored `cache/source-data/census-tiger-2020/`; receipts are in `project/data/manifests/source_manifest_v1.json` | Complete: both downloads exit 0; exact byte sizes verified; SHA-256 `fb91d692…afead9b` (ZCTA) and `a490d331…559d8d` (County); acquisition contract test passes |
| Census TIGER/Line extraction/schema experiment | Primary agent | `C:/Users/raede/Desktop/essay help master/Practicum/cache/logs/census-tiger-2020-schema.log`; extracted layers remain in ignored `cache/source-data/census-tiger-2020/extracted/`; tracked validation receipt is in `source_manifest_v1.json` | Complete: ZCTA 33,791/33,791 valid geometries with `ZCTA5CE20`; County 3,234/3,234 with `GEOID`; both EPSG:4269; acquisition/schema contract test passes |
| Stage 3 ensemble regeneration experiment | Primary agent | `C:/Users/raede/Desktop/essay help master/Practicum/cache/logs/stage3-ensemble-20260805.log`; inputs are verified Census cache plus synchronized Stage 2 artifacts; output is isolated under ignored `cache/experiments/stage3-ensemble-20260805/output/` | Complete: 1,002 ZIP-event rows across 22 events; formal Stage 3 hashes unchanged; ensemble-band-3/Albers result is R-squared 0.5887 versus the donor RF-band-1/Mercator baseline. Facility counts match for all 1,002 keys; mean probability changes by 0.0253 absolute on average and mean area changes from 55.42 to 37.66 km2. |
| Census ACS 2022 ZCTA controls | Primary agent | `C:/Users/raede/Desktop/essay help master/Practicum/cache/logs/acs-zcta-2022-download.log`; normalized public-data CSV and receipt in `project/data/raw/` | Complete: explicit `summary-file` mode passed 12 contract tests and wrote 33,774 unique five-digit ZCTAs. Output SHA-256 is `2cae6c00…7c1df3`; all 1,002 panel rows match a ZCTA and 977 have both controls. The manifest records both official response hashes and the exact `860Z200US` geography prefix. |
| OpenStreetMap provenance experiment (`zeta_atlanta`) | Primary agent | `C:/Users/raede/Desktop/essay help master/Practicum/cache/logs/osm-zeta-atlanta-20260805.log`; output `cache/source-data/osm-20260805/zeta_atlanta/` | Complete: after adding a tested identifying User-Agent, the live Overpass run wrote 399 deterministic POIs and an endpoint/query/time/ODbL receipt; CSV SHA-256 `af791973…cfdc8eb`. The pinned donor cache has 404 rows/SHA-256 `90e18b2b…b0d2c59`, a five-row difference consistent with a mutable live OSM source and therefore not silently substituted into canonical Stage 2 inputs. |
| Stage 3 ensemble full-regression experiment | Primary agent | `C:/Users/raede/Desktop/essay help master/Practicum/cache/logs/stage3-ensemble-full-20260805.log`; input panel is the isolated ensemble/Albers output, ACS is the verified Census file, EAGLE-I stays local; output `cache/experiments/stage3-ensemble-full-20260805/` | Complete for non-spatial models with formal outputs unchanged: M1 R-squared 0.5887/adjusted 0.5794 (n=1,002); M1+ R-squared 0.7603/adjusted 0.7543 (n=977). M3 was explicitly skipped pending `libpysal`; M8 was explicitly skipped because the isolated raw input did not yet expose the verified county layer. |
| NOAA NHC HURDAT2 Atlantic acquisition | Primary agent | `C:/Users/raede/Desktop/essay help master/Practicum/cache/logs/hurdat2-atlantic-20260805.log`; normalized track CSV and receipt in `project/data/raw/` | Complete after two real-sample contract repairs (21-field records and `-99` missing wind): 55,605 track rows from 2,004 storms, with all nine required named storm-years present. Source SHA-256 `1b9b0c7b…5c37202`; normalized output SHA-256 `09712f29…87d3cce`. The manifest records the official 1851–2025 URL and 2026-02-27 update vintage. |
| Dashboard dependency/test/build validation | Primary agent | `C:/Users/raede/Desktop/essay help master/Practicum/cache/logs/dashboard-dependency-upgrade-verify-20260805.log`; working directory `project/nightlight-dashboard/` | Complete: Vite 6.4.3, Vitest 3.2.7, PostCSS 8.5.25, and protocol-buffers-schema 3.6.1. Clean install and audit report zero vulnerabilities; final Vitest 17/17 and production build pass on Node 20.20.2. No deployment occurred. |
| Python spatial-regression dependencies | Primary agent | `C:/Users/raede/Desktop/essay help master/Practicum/cache/logs/python-spatial-deps-install-20260805.log`; isolated `.venv` only | Complete: installed and import-verified `libpysal 4.15.0`, `esda 2.10.0`, and `spreg 1.9.1`. These versions will be recorded in the Stage 2/3 reproducibility environment rather than inferred from the operator machine. |
| Stage 3 ensemble spatial/full validation | Primary agent | `C:/Users/raede/Desktop/essay help master/Practicum/cache/logs/stage3-ensemble-full-spatial-20260805.log`; isolated input assembly uses verified Census shapes, ACS, local EAGLE-I, and pinned spatial packages | Complete with formal hashes unchanged: M1 0.5887, M1+ 0.7603, M3 pseudo-R-squared 0.1528, M8 0.7636, M8+ 0.7593. M3 emitted a 20-component KNN warning and the severity join emitted a geographic-centroid warning, so spatial/severity metrics remain review candidates rather than publication claims pending targeted repairs. |
| Stage 3 ensemble extra-regression validation | Primary agent | `C:/Users/raede/Desktop/essay help master/Practicum/cache/logs/stage3-ensemble-extra-20260805.log`; normalized NOAA HURDAT2 track file is selected explicitly | Complete with formal hashes unchanged. All 554 hurricane observations received wind exposure. The regenerated descriptive facility-density Q3/Q1 ratio is 0.551 rather than donor 0.63; M5+ R-squared is 0.7825 (n=536), and residual Moran's I remains 0.3375 after controls. Causal/equity language remains blocked pending statistical review. |
| Formal Stage 3 regeneration | Primary agent | Inputs: `cache/experiments/stage3-inputs-20260805-v1`; formal outputs: `project/data/result/stage3/`; SHA-256 values in `canonical_results_v1.json` | Complete: 1,002 rows/22 events; full and extra regressions regenerated after statistical fixes. A final isolated rerun after fixing Moran row alignment and deterministic permutation inference reproduced the formal extra-result SHA-256 `c03c4959…49c7db`. M1+ remains 0.7603/0.7543; M3 has 22 event blocks and zero cross-event edges but remains diagnostic-only; M6/M7 are not identifiable; 0.551 is descriptive-only. |
| Dashboard final browser smoke | Primary agent | Final isolated local preview on `127.0.0.1:43174`; Playwright evidence is ignored local tooling output | Complete and stopped: Maria probability/facility resources loaded before switching to Satellite; the glyph-free style retained one canvas with no warning, console error, or page error. An injected Positron style failure produced the expected error/zero-canvas state, then Voyager restored one canvas and cleared the error with no page exception. Earlier route, lock, and 1,380 px LOEO checks remain passed. Port 43174 is released. |
| Final local validation | Primary agent | Fresh commands after all review fixes | Complete: Python `85 passed, 7 subtests passed`; compileall passed; seven tracked JSON documents parsed; four formal SHA-256 values match `canonical_results_v1.json`; npm audit reports zero vulnerabilities; Vitest 17/17 and Vite production build pass; independent final code review is APPROVE. |

## Handoff

Implementation and validation are complete. The functional implementation is committed locally as `acc4b2def96a4e653a604f33e8f462472fe1671d`; this record closes the local synchronization workflow. Public push and Pages deployment remain blocked by the EAGLE-I tracked-data/history decision.

## Next step

Before any public push or Pages deployment, resolve EAGLE-I redistribution rights and choose a clean-repository or controlled-history-remediation path, then run a separate publication review.
